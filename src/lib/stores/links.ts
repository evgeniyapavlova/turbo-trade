import { writable } from 'svelte/store';

export const login = writable('https://trade.turbo.trade/en/login');

export const reg = writable('https://trade.turbo.trade/en/register');

export const searchString = writable('');
